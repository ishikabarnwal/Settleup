# The datasource comes from the Postgres container in PostgresTestConfig.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false

app.jwt.secret=test-secret-key-used-only-by-the-test-suite
