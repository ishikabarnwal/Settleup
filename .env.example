# Copy to .env and fill in for local runs. Never commit the real .env.
DB_URL=jdbc:postgresql://localhost:5432/settleup
DB_USERNAME=settleup
DB_PASSWORD=settleup

# Must be at least 32 characters.
JWT_SECRET=change-me-to-a-long-random-string-32+
